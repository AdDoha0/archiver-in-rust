📌 1. main.rs (Точка входа)
Разбирает аргументы командной строки (clap).
Вызывает нужные функции (pack, unpack).


📌 2. commands.rs (Обработка команд)
Определяет логику для pack и unpack.
Взаимодействует с модулями zip.rs / tar.rs.


📌 3. archive/ (Работа с архивами)
zip.rs → Упаковка и распаковка .zip.
tar.rs → Упаковка и распаковка .tar.gz.

📌 4. utils.rs (Вспомогательные функции)
Проверка существования файлов/папок.
Логирование, обработка ошибок.
